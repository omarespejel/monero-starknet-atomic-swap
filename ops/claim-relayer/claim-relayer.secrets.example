# Copy to /etc/atomic-swap/claim-relayer.secrets on the Monero VM.
# Never commit the real file. chmod 600 and keep one env var per lock.

RELAYER_PARTIAL_SEPOLIA_STRK_SMOKE=REPLACE_WITH_32_BYTE_HEX_PARTIAL_SPEND_KEY
