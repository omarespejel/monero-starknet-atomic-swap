#!/usr/bin/env python3
"""Legacy DLEQ hint generator from Rust test vectors."""

raise SystemExit(
    "generate_hints_from_test_vectors.py is disabled because it targets the legacy "
    "truncated DLEQ path. Use `uv run --project tools python tools/regenerate_dleq_hints.py`."
)
