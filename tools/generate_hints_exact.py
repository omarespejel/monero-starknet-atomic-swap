#!/usr/bin/env python3
"""Legacy exact-coordinate DLEQ hint generator."""

raise SystemExit(
    "generate_hints_exact.py is disabled because it targets the legacy truncated DLEQ path. "
    "Use `uv run --project tools python tools/regenerate_dleq_hints.py` instead."
)
